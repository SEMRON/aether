syntax = "proto3";

import "runtime.proto";

service SwarmConnectionHandler {
  // Listens to incoming requests for expert computation
  rpc info(ExpertUID) returns (ExpertInfo);
  rpc forward(ExpertRequest) returns (ExpertResponse);
  rpc backward(ExpertRequest) returns (ExpertResponse);
  // Forward pass using globally averaged model weights (for distributed RL policy inference)
  rpc forward_averaged(ExpertRequest) returns (ExpertResponse);
}

message SwarmExpertRequest {
  string uid = 1;
  repeated Tensor tensors = 2;
}

message SwarmExpertResponse {
  repeated Tensor tensors = 2;
}
